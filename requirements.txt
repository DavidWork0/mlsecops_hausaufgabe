# Safe requirements for Docker builds
# Dependencies that aren't installed separately in the Dockerfile
plotly
ipython
boto3
tabulate
psutil
PyYAML
joblib
threadpoolctl
#apache-airflow  # Commented out as it's heavy and can cause issues